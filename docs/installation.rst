.. highlight:: shell

============
Installation
============


Stable release
--------------

To install HousingPrice, run this command in your terminal:

.. code-block:: console

    $ pip install HousingPrice

This is the preferred method to install HousingPrice, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for HousingPrice can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/DebugJedi/HousingPrice

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/DebugJedi/HousingPrice/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/DebugJedi/HousingPrice
.. _tarball: https://github.com/DebugJedi/HousingPrice/tarball/master
